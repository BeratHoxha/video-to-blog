<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <title>Video To Blog — Turn any video into a polished blog post</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Paste a URL or drop a file. Choose your style. Get a blog post in seconds.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= vite_client_tag %>
    <%= vite_react_refresh_tag %>
    <%= vite_javascript_tag 'main.tsx' %>

    <script>
      window.__RAILS_ENV__ = {
        currentUser: <%= raw(current_user_json.to_json) %>,
        csrfToken: "<%= form_authenticity_token %>"
      };
    </script>
  </head>
  <body class="bg-gray-950 text-white font-sans antialiased">
    <% if notice %>
      <div id="flash-notice" class="fixed top-4 right-4 z-50 bg-emerald-500 text-white px-4 py-2 rounded-lg text-sm transition-opacity duration-500">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div id="flash-alert" class="fixed top-4 right-4 z-50 bg-red-500 text-white px-4 py-2 rounded-lg text-sm transition-opacity duration-500">
        <%= alert %>
      </div>
    <% end %>
    <script>
      (function() {
        function fadeOut(el) {
          if (!el) return;
          setTimeout(function() {
            el.style.opacity = "0";
            setTimeout(function() { el.remove(); }, 500);
          }, 5000);
        }
        fadeOut(document.getElementById("flash-notice"));
        fadeOut(document.getElementById("flash-alert"));
      })();
    </script>
    <%= yield %>
  </body>
</html>
